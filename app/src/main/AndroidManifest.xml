<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="net.ambrose.golf_90105">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".chooseLanguage"
            android:label="Languages"/>
        <activity android:name=".GoogleMapActivity"
            android:label="@string/maps_label"/>

        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyBir8Dx1h5jzJUc0pDLz-oZIZRu-Uafu_s" />

        <activity android:name=".Services2"
            />
        <activity android:name=".Services1"
            />
        <activity android:name=".ClubServices"
            android:label="Services"/>
        <activity android:name=".ProfileActivity"
            android:label="User Profile"/>
        <activity android:name=".LoginActivity"
           />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>